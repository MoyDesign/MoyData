{{!--
name: Gray low-contrast feed
type: feed
author: Moy.Design
description: Minimalistic low-contrast template with gray background.
  Displays a blog feed in a human-friendly way without needless formatting.
  Inspired by http://bettermotherfuckingwebsite.com/
--}}
<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <base href="{{ BASE_URL }}">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ TITLE }}</title>
  {{{ ICON_TAGS }}}
  {{{ OPEN_GRAPH_TAGS }}}
  <style>
    body {
      line-height: 1.6;
      font-size: 20px;
      color: #444;
      padding:0 10px;
      background-color: #eee;
    }
    .container {
      margin: 20px auto;
      max-width: 800px;
    }
    .article-body img {
      margin: 0.5em auto;
      display: block;
      height: auto;
      max-width: 100%;
    }
    .article-body a img {
      margin: inherit;
      display: inherit;
      height: inherit;
    }
    .article-body iframe {
      max-width: 100%;
      width: 640px;
      height: 360px;
    }
    .author_pic {
      float: left;
      margin-right: 0.5em;
    }
    .article-body {
      clear: both;
    }
    .article-body div {
      margin-top: 1em;
      margin-bottom: 1em;
    }
    h1 {
      font-size: 1.25em;
    }
    h2 {
      font-size: 1.125em;
    }
    h3 {
      font-size: 1.05em;
    }
    a, a:visited {
      color: #0074D9;
      text-decoration: underline;
    }
    .article-header a {
      text-decoration: none;
    }
    .article-body a:visited {
      color: #B10DC9;
    }
    hr {
      width: 70%;
      margin: 2em auto;
    }
    .nav {
      margin: 0 auto;
      text-align: center;
    }
    .nav-prev, .nav-middle, .nav-next {
      display: inline-block;
    }
    .title {
      margin: 0;
      line-height: 1.2;
    }
    .date {
      color: #888;
      font-size: smaller;
    }
    .new-comment {
      margin-left: 1em;
    }
    .logo {
      float: right;
    }
    .logo img {
      max-width: 50px;
      max-height: 50px;
    }
    details[open] span.summary-closed {
      display: none;
    }
    details:not([open]) span.summary-closed {
      display: inline-block;
    }
    summary {
      cursor: pointer;
    }
    details {
      border-top: 1px solid #444;
      margin-bottom: 1em;
    }
    details:not([open]) {
      border-top: none;
      border-bottom: none;
    }
  </style>
</head>
<body>
  <div class='container'>
    <div class='logo' style='float: right;'>
      {{#if logo_small_img_src}}
        <a href="{{#if logo_small_link}}{{logo_small_link}}{{else}}/{{/if}}">
          <img src="{{logo_small_img_src}}">
        </a>
      {{/if}}
    </div>
    <div class='articles'>
      {{#each article}}
        <details open>
          <summary>
            <span class='summary-closed'>
              {{#if author}}
                {{#if author_link}}
                  <a href="{{author_link}}">{{{author}}}</a>:
                {{else}}
                  {{{author}}}:
                {{/if}}
              {{/if}}
              {{#if title_link}}
                <a href="{{title_link}}">{{{title}}}</a>
              {{else}}
                {{title}}
              {{/if}}
            </span>
          </summary>
          <div class='article-header'>
            <div class='author_pic'>
              {{#if author_img_src}}
                {{#if author_link}}
                  <a href="{{author_link}}"><img src="{{author_img_src}}"></a>
                {{else}}
                  <img src="{{author_img_src}}">
                {{/if}}
              {{/if}}
            </div>
            <div class='author'>
              {{#if author_link}}
                <a href="{{author_link}}">{{{author}}}</a>
              {{else}}
                {{{author}}}
              {{/if}}
            </div>
            <h1 class='title'>
              {{#if title_link}}
                <a href="{{title_link}}">{{{title}}}</a>
              {{else}}
                {{title}}
              {{/if}}
            </h1>
            <div class='date'>{{date}}</div>
          </div>
          <div class='article-body'>{{{body}}}</div>
          <div class='article-comments'>
            <span class='comments'>
              {{#if comments}}
                {{#if comments_link}}
                  <a href="{{comments_link}}">{{{comments}}}</a>
                {{else}}
                  {{{comments}}}
                {{/if}}          
              {{/if}}
            </span>
            <span class='new-comment'>
              {{#if new_comment}}
                {{#if new_comment_link}}
                  <a href="{{new_comment_link}}">{{{new_comment}}}</a>
                {{else}}
                  {{{new_comment}}}
                {{/if}}          
              {{/if}}
            </span>
          </div>
        </details>
      {{/each}}
    </div>
    <div class='nav'>
      <span class='nav-prev'>
        {{#if nav_prev}}
          {{#if nav_prev_link}}
            <a href="{{nav_prev_link}}">{{{nav_prev}}}</a>
          {{else}}
            {{{nav_prev}}}
          {{/if}}          
        {{/if}}
      </span>
      <span class='nav-middle'>
        {{#if nav_middle}}
          {{#if nav_middle_link}}
            <a href="{{nav_middle_link}}">{{{nav_middle}}}</a>
          {{else}}
            {{{nav_middle}}}
          {{/if}}          
        {{/if}}
      </span>
      <span class='nav-next'>
        {{#if nav_next}}
          {{#if nav_next_link}}
            <a href="{{nav_next_link}}">{{{nav_next}}}</a>
          {{else}}
            {{{nav_next}}}
          {{/if}}          
        {{/if}}
      </span>
    </div>
  </div>
</body>
</html>
